version: "1.0"
linter: qodana-dotnet

bootstrap: |
  dotnet --info
  dotnet restore "./FileClassifier.sln"
  dotnet build "./FileClassifier.sln" -c Release --no-restore

dotnet:
  solution: "./FileClassifier.sln"
